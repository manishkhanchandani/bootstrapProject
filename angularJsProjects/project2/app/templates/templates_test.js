'use strict';

describe('templateStore.templates module', function() {

  beforeEach(module('templateStore.templates'));

  describe('templates controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var templatesCtrl = $controller('TempatesCtrl');
      expect(templatesCtrl).toBeDefined();
    }));

  });
});